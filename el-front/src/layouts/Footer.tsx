import React from 'react'

import styles from '@stylesLayouts/Footer.module.scss';

const Footer = () => {
  return (
    <footer className={styles.wrapper}>
      Footer
    </footer>
  )
}

export default Footer